<header>

  <% if current_user %>
  <ul>
    <%= link_to(current_user.name, users_path(current_user))%>
    <hr>
    <%= link_to('Sign out', signout_path, :method => :delete)%>
  </ul>

  <% end %>
</header>